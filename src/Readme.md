# BSF-NUTRIFIED-PROJECT

# Dependencies

- express
- dotenv

# Project Structure

src/
controllers/
middleware/
models/
app.js

# Environment Variables

Create a .env file in the root folder:
PORT=3000

# Setup Instructions

1. Install dependencies
   npm install

2. Run the project
   npm start

Server runs on http://localhost:3000
